version = file('version.txt').text.trim()

allprojects {
    group 'ai.hyacinth.framework'
    description "Core Service Framework"
    version = rootProject.version
}

apply from: './build-script.gradle'
