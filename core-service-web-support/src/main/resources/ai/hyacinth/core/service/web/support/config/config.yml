spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB
      file-size-threshold: 5MB
  #  mvc:
  #    static-path-pattern: /resources/**

management:
  endpoints:
    web:
      #      base-path: "/"
      exposure:
        include: "*"
      #  server:
      #    port: 8081
